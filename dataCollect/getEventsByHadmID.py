import os
import pandas as pd
import numpy as np
import multiprocessing



def get_event_hadmid(hadm_ids, output_path=None):
    '''
    Currently, similar functionality happens with extract_subjects.py
    If we want to integrate this and do this correctly, we should probably be able to do this in our own project?
    :param hadm_ids a list of all hadm_ids to go through and retrieve events for
    :param output_path a directory to write to, where each folder is an hadm_id, if none, return an array of dfs instead 
    '''
    #TODO
    return None
    